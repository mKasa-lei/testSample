TestCafeのサンプルです。

ブラウザが立ち上がり、グーグルへ行って、検索の件数表示が存在してたらテスト完了という<br/>簡単なテストを書いています。<br/>
準備として、<br/><br/>
yarnが使えるようにして、yarn add -g testcafeでグローバルインストールし、<br/>yarn installをたたき(yarn のみでも可)<br/>
testSample.exeをクリックでいけるはずです。<br/>
(node index.jsをコマンドで打っても同じく動きます)
<br/><br/>
できなそうでしたら、ご連絡をください。
